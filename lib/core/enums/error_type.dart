enum ErrorType {
  network,
  noInternet,
  timeout,
  cancelled,
  badRequest,
  authentication,
  authorization,
  notFound,
  validation,
  rateLimit,
  server,
  firebaseAuth,
  firebaseStore,
  storage,
  security,
  localStorage,
  unknown,
}
